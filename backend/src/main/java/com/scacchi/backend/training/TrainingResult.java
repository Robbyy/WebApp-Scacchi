package com.scacchi.backend.training;

/** Esito di una sessione di allenamento (Prototipo 17). */
public enum TrainingResult {
    /** La variante è stata completata fino in fondo. */
    COMPLETED,
    /** La sessione è stata interrotta prima della fine. */
    ABANDONED
}
