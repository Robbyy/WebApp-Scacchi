package com.scacchi.backend.study;

/**
 * Colore/orientamento di uno studio (Prototipo 11). A differenza del colore di
 * una singola variante (solo WHITE/BLACK), uno studio può raccogliere un
 * repertorio per entrambi i lati: {@code MIXED}. Il campo è opzionale.
 */
public enum StudyColor {
    WHITE,
    BLACK,
    MIXED
}
